FROM node:22-bookworm-slim

WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci
RUN mkdir -p /app/node_modules/.vite-temp

# Generated code and reviewed tests are mounted read-only at /app/work.
WORKDIR /app/work
